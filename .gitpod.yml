image:
  file: Dockerfile

tasks:
  -  before: mkdir android \ &&
             touch logs.txt
             
             
     command: echo "ls -la"          2>&1 >> logs.txt \ &&     
              ls -la                 2>&1 >> logs.txt \ &&  
              echo                   2>&1 >> logs.txt \ &&    
              echo "pwd"             2>&1 >> logs.txt \ &&       
              pwd                    2>&1 >> logs.txt  

    
    
 
